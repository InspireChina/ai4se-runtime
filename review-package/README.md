# review-package/

**Canonical templates live in [`templates/review/`](../templates/review/README.md).**

This directory is kept for compatibility with `ai4se-review-tools` generated output paths.
Prefer editing files under `templates/review/`; sync here if the tool still reads this path.
